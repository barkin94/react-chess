import { MoveManager } from "../interfaces/move-manager.interface";
import { Coordinates } from "../types/coordinates.type";

export class PawnMoveManager implements MoveManager {
  getMovableCoordinates(): Coordinates[] {
    const movableLocations: Coordinates[] = [];
    //while(location.y)
    return movableLocations;
  }
}
